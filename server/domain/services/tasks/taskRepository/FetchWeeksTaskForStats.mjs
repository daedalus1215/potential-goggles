import { getDatesOfPastWeek } from "../../../../utils/getDate.mjs";

export const FetchWeeksTaskForStats = (tasks) => {
    const dates = getDatesOfPastWeek(new Date());
    tasks.map()
};

export default FetchWeeksTaskForStats;
